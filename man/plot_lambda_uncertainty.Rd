% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting_functions.R
\name{plot_lambda_uncertainty}
\alias{plot_lambda_uncertainty}
\title{Figure 5 plotting function
Lambda uncertainty plot (per player)}
\usage{
plot_lambda_uncertainty(
  fit,
  w_ij = NULL,
  players_df = NULL,
  players_id_col = NULL,
  labels = NULL,
  labels_key_col = NULL,
  labels_value_col = NULL,
  clean_display_labels = NULL,
  order_ids = NULL,
  log_base = exp(1),
  max_n_clust = NULL,
  prob = 0.9,
  palette = NULL,
  clean_fun = function(x) x,
  x_hat = NULL,
  filter_lambdas = TRUE,
  conditional = TRUE,
  ...
)
}
\arguments{
\item{fit}{Output from \code{gibbs_BT_SBM()} with \code{opt_lambda$lambda_item} computed
(set \code{keep_lambda=TRUE} when sampling).}

\item{w_ij}{Optional wins matrix to compute marginal wins for ordering and annotation.}

\item{log_base}{Base for the x-axis logarithm (10 or e). Defaults to 10.}

\item{max_n_clust}{Where to filter the mcmc x_t. If not specified we use the modal K}

\item{prob}{Interval probability for HPD (e.g., 0.90).}

\item{palette}{Named colors for clusters.}

\item{clean_fun}{Optional player-name cleaner. Default: identity.}
}
\value{
A \code{ggplot} object.
}
\description{
Forest plot of eqn{\lambda} with uncertainty intervals,
using relabeled draws. Points colored by the hard partition (\code{fit$estimates$x_hat}).
}
\examples{
\dontrun{
# fit with keep_lambda=TRUE
p <- plot_lambda_uncertainty(fit, prob = 0.90)
}
}
