% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting_functions.R
\name{exploratory_adjacency}
\alias{exploratory_adjacency}
\title{Adjacency heatmap (wins per matches)}
\usage{
exploratory_adjacency(
  Y_ij,
  N_ij = NULL,
  names = NULL,
  last_rank = NULL,
  order_by = c("auto", "none", "last_rank"),
  players_df = NULL,
  players_id_col = NULL,
  players_rank_col = NULL,
  labels = NULL,
  labels_key_col = NULL,
  labels_value_col = NULL,
  clean_display_labels = NULL,
  clean_fun = clean_players_names,
  max_wins = 4L,
  show_margins = TRUE,
  legend_title = "Number of wins",
  no_match_label = "No match played",
  no_match_color = "white",
  mark_unplayed = TRUE,
  unplayed_mark = "×",
  unplayed_mark_size = 2.2,
  bw_preview = TRUE
)
}
\arguments{
\item{Y_ij}{Integer matrix of wins. Entry (i, j) is wins of i over j.}

\item{N_ij}{Integer matrix of matches. If \code{NULL}, computed as \code{Y_ij + t(Y_ij)}.}

\item{names}{Optional character vector of player names to include. Can be
either raw matrix dimnames (slugs) or display names (after cleaning).
If \code{NULL}, plots all players.}

\item{last_rank}{Optional numeric vector of year-end ranks. Can be named
(names are player identifiers) or an unnamed vector aligned with the players
being plotted.}

\item{order_by}{Ordering criterion. \code{"last_rank"} orders by ascending rank
(rank 1 at the top). \code{"none"} keeps matrix order. \code{"auto"} uses
\code{"last_rank"} if ranking info is provided, otherwise \code{"none"}.}

\item{players_df}{Optional data.frame with columns \code{player_slug} and \code{last_rank}
(e.g., season-level metadata). Used when \code{order_by = "last_rank"}.}

\item{clean_fun}{Name-cleaning function applied to display labels.}

\item{max_wins}{Maximum win count shown explicitly in the legend; larger
counts are bucketed into \code{paste0(max_wins, "+")}.}

\item{show_margins}{If \code{TRUE} and package \code{ggside} is available, draws a
side bar with total matches per player.}
}
\value{
A \code{ggplot} object.
}
\description{
Exploratory adjacency heatmap showing wins for each ordered pair of players.
Optionally restrict to a subset of players and/or order rows/cols by an
external metric such as year-end ranking.
}
