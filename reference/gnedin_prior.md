# Gnedin prior for a latent partition

Gnedin prior for a latent partition

## Usage

``` r
gnedin_prior(gnedin_hyperparameter = 0.5)
```

## Arguments

- gnedin_hyperparameter:

  Gnedin hyperparameter in `(0, 1)`.

## Value

An object of class `btsbm_partition_prior`.
